sudo useradd -s /sbin/nologin -r nginx
sudo addgroup web
sudo usermod -a -G web nginx
sudo apt-get install postfix
sudo adduser static
sudo adduser static adm
sudo adduser static sudo
\curl -L https://get.rvm.io | bash -s stable --rails --autolibs=enabled

# Not totally sure whether Digital Ocean's initial load included node.js.
# But it seems to be needed for one thing or another, maybe the login stuff
# I tried in the BRVE project.  So for testing on an envelopment system,

# sudo apt-get install -y nodejs
